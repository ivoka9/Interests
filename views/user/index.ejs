<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles/login.css" />
    <script
      defer
      src="https://code.jquery.com/jquery-3.5.1.min.js"
      integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
      crossorigin="anonymous"
    ></script>
    <% if (usernameTakenFlag){ %>
    <script defer src="scripts/register.js"></script>
    <% }else{ %>
    <script defer src="scripts/login.js"></script>
    <% } %>
    <title>Login</title>
  </head>

  <body>
    <div class="hero">
      <div class="hero-box">
        <div class="hero-box-button">
          <div id="hero-box-button-active"></div>
          <button type="button" class="hero-box-button-toggle" id="login-btn">
            Log In
          </button>
          <button
            type="button"
            class="hero-box-button-toggle"
            id="register-btn"
          >
            Register
          </button>
        </div>
        <form
          id="login"
          class="hero-box-form"
          action="/interests"
          method="POST"
        >
          <% if( accCreated ) { %>
          <p style="color: green;">Your account was sucessfuly created</p>
          <% }%>
          <input
            type="text"
            required
            class="hero-box-form-input"
            placeholder="Enter your Username"
            name="username"
          />

          <input
            type="password"
            required
            class="hero-box-form-input"
            placeholder="Enter your Password"
            name="password"
          />
          <% if (wrongUsernameOrPassword){ %>
          <p style="color: red;">Wrong Username or Passsword</p>
          <% } %>
          <input class="hero-box-form-submit" type="submit" value="Log In" />
        </form>
        <form id="register" class="hero-box-form" action="/login" method="POST">
          <input
            type="text"
            required
            class="hero-box-form-input"
            placeholder="Enter a Username"
            name="username"
          />
          <% if (usernameTakenFlag){ %>
          <p style="color: red;">This username has been taken</p>
          <% } %>
          <input
            type="password"
            required
            class="hero-box-form-input"
            placeholder="Enter a Password"
            name="password"
          />
          <input class="hero-box-form-submit" type="submit" value="Register" />
        </form>
      </div>
    </div>
  </body>
</html>
